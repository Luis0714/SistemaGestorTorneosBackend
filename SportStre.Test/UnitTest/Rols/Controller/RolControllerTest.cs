using MediatR;
using Moq;
using ShopEstre.Application.Roles.Commands.CreateRol;
using ShopEstre.Domain.Dtos.Rols;
using System;

namespace SportStre.Test.UnitTest.Rols.Controller
{
    public class RolControllerTest
    {
        [Test]
        public void CreateRolTest()
        {
            //var mediatorMock = new Mock<IMediator>();

            ////asignar comportamiento
            //mediatorMock.Setup(mock => mock.Send(new CreateRolCommand() { RolId = null, Name = "Name", Description = "Desc"});
            //.Result.Returns(new RolDto() { RolId = Guid.NewGuid(), Name = "Name", Description = "Desc" });

            //RolController controller = new ()

        }

    }
}
